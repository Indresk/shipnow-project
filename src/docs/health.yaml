paths:
  /:
    get:
      summary: Health check de la API con información de estado
      tags:
        - Health
      responses:
        '200':
          description: API corriendo exitosamente con información de estado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
